Django>=5.0,<6.0
djangorestframework>=3.15
django-cors-headers>=4.3
psycopg2-binary>=2.9  # only needed when POSTGRES_DB is set
Pillow>=10.0
gunicorn>=22.0  # production WSGI server
whitenoise>=6.6  # serves static files (incl. admin) without a separate web server
