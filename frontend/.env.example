VITE_API_URL=http://127.0.0.1:8000/api

# Powers the "Find a song" tab (real songs via YouTube, not just previews).
# Free, one-time setup, no payment — see the top of src/utils/youtube.js
# for the exact steps. The app works fine without this; that tab just shows
# a setup hint instead of search results.
VITE_YOUTUBE_API_KEY=